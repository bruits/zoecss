[package]
name = "zoecss-presets"
version = "0.1.0"
description = "Composable config presets for ZoeCSS"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
zoecss-core = { path = "../zoecss-core" }
zoecss-config = { path = "../zoecss-config" }
regex = "1"

[lib]
