[package]
name = "zoecss-config"
version = "0.1.0"
description = "Configuration model, merging, and compilation for ZoeCSS"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
zoecss-core = { path = "../zoecss-core" , version = "0.1.0" }
regex = "1"
rustc-hash = "2"
thiserror = "2"

[lib]
